package Model;

import java.io.File;

// A standard class that holds the model which we will use for simulation.
public class Model {
    private File modelFile;
    public Model(File input){
        modelFile = input;
    }
}
